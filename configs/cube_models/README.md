# Cube Models

`cube_model_hybrid_v1.json` is the canonical definition for the physical ArUco cube used in this project.

Use this model as the default across capture, recalibration, verification, and export.
Only pass `--cube_config_json` when you intentionally switch to a different physical cube.
